// // @ts-check

// import pageInfo from "utils/page-info";
// import { ObjectEntries } from "utils/helpers";
// import { test, expect } from "@playwright/test";
// import { PageInfo } from "interfaces/utils/page-info";

// test("can user signin", async ({ page }) => {
//   await page.getByLabel("Email Address").fill("maduekwepedro@gmail");
//   await page.getByLabel("Password").fill("6565656Lagos");
//   await page.get("Password").fill("6565656Lagos");
// });

// // test("get started link", async ({ page }) => {
// //   await page.goto("/");

// //   // Click the get started link.
// //   await page.getByRole("link", { name: "Get started" }).click();

// //   // Expects page to have a heading with the name of Installation.
// //   await expect(page.getByRole("heading", { name: "Installation" })).toBeVisible();
// // });

// // test("get started link", async ({ page }) => {
// //   await page.goto("/");

// //   // Click the get started link.
// //   await page.getByRole("link", { name: "Get started" }).click();

// //   // Expects page to have a heading with the name of Installation.
// //   await expect(page.getByRole("heading", { name: "Installation" })).toBeVisible();
// // });
