import Carousel from "nuka-carousel";
import { Typography } from "@mui/material";

import { peaksStyles } from ".";

const Peaks = () => {
  return (
    <div className={peaksStyles.transparent}>
      "No Player hoarding in any particular team", "Competitive transfer market", "Advanced tactics and formation", "Real match simulation", "Players with
      rating over '89' are registered as Free agents",
    </div>
  );
};

export default Peaks;
